# Architecture — Innovate Inc

> **Not written yet.** This directory will hold the infrastructure design for Innovate Inc.
> It is a placeholder so the repository structure is complete; there is no content to review here.

Planned contents:

- Cloud environment structure — the smallest sensible account separation to start with, and the
  trigger that justifies each additional account
- Network architecture
- Security
- Managed Kubernetes: choice and cluster design
- Node strategy, autoscaling, resource allocation
- Container build, registry, deployment
- PostgreSQL, high availability, backups, disaster recovery
- High-level architecture diagram

The Terraform proof of concept is in [`../terraform/`](../terraform/) and is independent of this
work.
